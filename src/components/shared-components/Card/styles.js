/**
 * Card Stylesheet in JSS
 */
const cardBackgroundColor = "#ffffff";
const cardShadowColor = "#4c4c4c";

const styles = {
  card: {
    backgroundColor: cardBackgroundColor,
    borderRadius: "5px",
    width: "100%",
    padding: "10px",
    boxShadow: `1px 1px 3px ${cardShadowColor}`,
  },
};

export default styles;
